{%- for section, section_settings in settings.items() %}
[{{ section }}]
{%-   for setting in section_settings %}
{%-     set k, v = setting.items()[0] %}
{{ k }}={{ v }}
{%-   endfor %}
{%- endfor %}
